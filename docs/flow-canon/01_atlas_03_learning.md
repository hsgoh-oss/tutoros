# Atlas 체크리스트 — 과제·시험·진도 (H·A·G)

> 정본: TutorOS_Visual_Flow_Atlas.docx (SHA-256 03BC551C…8301) · 체크 = 해당 흐름이 코드로 실제 동작함(구현 완료) · 갭: ✅ 있음 / 🔶 부분 / ❌ 없음 / ⚠️ 충돌(정본 우선)

## 11. 과제·제출·피드백·질의응답 (H-01–H-07)

### H-01 과제 생성·배부
- [x] **구현 완료** — 수업기록·학습목표·평가 결과에서 과제 초안을 만들어 운영자 검토·게시 후 대상 학생 배부·알림까지 코드로 동작한다.
- **주 전환:** 수업기록·학습목표·평가 결과 → 과제 초안 → 운영자 검토 → 게시 → 대상 학생 배부 → 학생 알림
- **예외·복구·종료:**
  - [x] 초안: 학생·보호자 비노출.
  - [ ] 일부 배부 실패: 성공 대상 유지 → 실패 대상만 재처리.
  - [ ] 잘못된 대상: 배부 철회 → 올바른 대상에 새 배부.
  - [ ] 게시 후 내용 변경: 새 과제본 → 재검토·재게시.
- **현행 갭:** 🔶 부분(판정 2026-08-25, 커밋 4ec60e4) — `supabase/migrations/00015_homework.sql:27` · `app/admin/(protected)/homework/actions.ts:80`·`:117`·`:158`·`:212`·`:228` · `lib/data/homework.ts:483` — 과제 엔티티(draft→assigned)·초안 비노출(포털 조회는 assigned·closed만)·게시·배부·학부모 알림(큐 적재 실측)·배부 철회(제출 전만 — 원자 RPC `00015:232`)·게시 후 변경은 새 과제본 안내까지 E2E 실측 완주 — 남은 갭: 근거가 수업 연결(lesson_id)뿐이라 학습목표·평가 결과발 초안 생성 없음(G·A 도메인 부재), 과제가 학생 1인 단위라 다인 배부·일부 배부 실패 재처리 개념 없음.

### H-02 과제 제출·재제출
- [x] **구현 완료** — 학생이 배부 과제를 확인하고 파일·내용 검사를 거쳐 제출 완료 후 운영자 검토 업무로 넘어가는 흐름이 동작한다.
- **주 전환:** 학생이 배부 과제 확인 → 제출 시작 → 파일·내용 검사 → 안전한 항목 확정 → 제출 완료 → 운영자 검토 업무
- **예외·복구·종료:**
  - [ ] 일부 업로드 실패: 성공 항목 유지 → 실패 항목만 다시 제출.
  - [ ] 안전하지 않은 파일: 격리 → 거부 안내 → 안전본 재제출.
  - [x] 마감 후 제출: 차단하지 않고 지연 사실과 실제 시각을 연결.
  - [x] 재제출: 이전 제출 보존 → 새 제출본을 최신 검토 대상으로 연결.
  - [ ] 제출 철회: 검토 전이면 철회, 검토 후면 운영자 정정 흐름.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `app/portal/[token]/actions.ts:151`·`:220`·`:228`·`:253`·`:256`·`:303`·`:383` · `supabase/migrations/00015_homework.sql:107` — 포털 제출(파일 10MB·확장자·MIME 검증)→제출 완료→운영자 검토 업무(work_items homework_submitted) 자동 생성까지 실측, 재제출은 append-only(attempt_no+1 새 행 — 원문 불변 DB 트리거, RLS 하네스 8l 절 `supabase/tests/rls/99_rls_test.sql:691`로 service_role까지 차단됨을 실증), 마감 후 제출은 차단 없이 late 플래그로 실제 시각 연결, 검토 전(pending) 최신 제출만 철회 허용. M1 역할별 포털이 들어오면서 같은 액션이 토큰·세션 두 경로로 갈리고(`app/portal/[token]/actions.ts:108` resolvePortalActor — 세션 경로는 active student 관계만 통과), 보호자 뷰에는 제출 폼을 가진 컴포넌트를 넣지 않는 방식으로 대리 제출을 막는다(`components/portal/homework-status-list.tsx:4`) — 남은 갭: 안전성(유해성) 검사·격리 없음(형식·크기 검증만), 검토 후 운영자 정정 흐름 없음. 실측은 토큰 경로 기준이고 세션 경로 제출·재제출은 미실측.

### H-03 채점·피드백·재제출 순환
- [x] **구현 완료** — 제출 확인부터 운영자 검토·채점, 피드백 초안, 공개 대상 확인, 승인·게시까지의 순환이 동작한다.
- **주 전환:** 제출 확인 → 운영자 검토·채점 → 피드백 초안 → 공개 대상 확인 → 승인·게시
- **예외·복구·종료:**
  - [x] 미승인 피드백: 미승인 피드백은 알림·포털 비노출.
  - [ ] 공개 범위 확인: 보호자 공개 범위와 학생 공개 범위를 확인한 뒤 게시.
  - [x] 철회 피드백 재게시: 철회한 피드백을 그대로 재게시하지 않고 새 본으로 처리.
  - 분기·상태 수렴 — 게시 결과: 완료 → 과제 종료 / 보완 필요 → 재제출 요청 → 학생 재제출 → 다시 검토 / 추가 설명 필요 → 질의응답 / 정정 필요 → 새 피드백본 → 재승인·재게시.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `app/admin/(protected)/homework/actions.ts:319`·`:382`·`:449` · `lib/data/homework.ts:450`·`:461` — 검토 확정(complete/보완 필요 resubmit)→피드백 초안(항상 draft — 미승인은 내용·존재 모두 포털 비노출)→승인 게시(draft만·경합 시 이중 승인 차단)→포털 승인본 노출까지 E2E 실측, 게시 피드백 수정은 감사 선기록과 함께 강등+새 초안(`:421`). M1 역할별 포털로 열람 표면은 갈렸다 — 학생 뷰는 제출·질문이 가능한 카드(`components/portal/homework-card.tsx:135`), 보호자 뷰는 승인 피드백만 읽는 현황 목록(`components/portal/homework-status-list.tsx:83`)이고 제출 파일 열람 경로가 없다 — 남은 갭: 갈린 것은 화면이지 공개 범위 판단이 아니다. 승인이 한 번이라 같은 피드백 문안이 학생·보호자 양쪽에 동시에 뜨고, 게시 전 "보호자 공개 범위/학생 공개 범위"를 나눠 정하는 단계는 없다(리포트의 audience 분리와 달리). 점수 채점 없이 판정·피드백 텍스트만인 것도 그대로.

### H-04 학생 질의응답
- [ ] **구현 완료** — 학생 질문 접수부터 원 기록 확인, 답변 초안, 운영자 승인·게시, 학생 확인까지 동작한다.
- **주 전환:** 학생이 수업·과제·문항·피드백 원본에서 질문 → 질문 접수 → 운영자 답변 업무 → 원 기록 확인 → 답변 초안 → 운영자 승인·게시 → 학생 확인
- **예외·복구·종료:**
  - [ ] 학생 추가 질문: 같은 원본 흐름에서 답변 순환.
  - [x] 해결 완료: 질문 닫기 → 학습 이력 유지.
  - [ ] 운영자 추가 과제 필요: 과제 흐름으로 연결.
  - [ ] AI 답변 초안: 승인 전 비노출.
  - [x] 다른 학생·무관계 보호자: 질문 존재 자체를 노출하지 않는다.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `app/portal/[token]/actions.ts:409`·`:500` · `app/admin/(protected)/homework/actions.ts:702`·`:826`·`:834` · `supabase/migrations/00015_homework.sql:146`·`:204` · `lib/data/homework.ts:572` — 질문 접수(원 과제·수업·리포트 연결 강제, 본인에게 노출되는 원 기록만, 타 테넌트 리포트 연결은 복합 FK 차단)→오늘 업무(work_items question_asked) 수렴→답변 초안(draft 비노출)→승인 게시/해결 닫기까지 코드 구현, 학생 뷰는 본인 질문·승인된 답변만 조회(`lib/portal/data.ts:152`) — 실측은 접수 폼 렌더만이며 답변→승인→학생 확인 순환은 실측 없음, AI 답변 초안은 미구현(수동 답변만).

### H-05 기존 학습이력 가져오기·대사
- [ ] **구현 완료** — 기존 진도·평가·파일·메모의 원본·범위 확정부터 모의 검토, 후보 분리, 승인, 학습이력 반영, 학생별 결과 대사까지 동작한다.
- **주 전환:** 가져올 기존 진도·평가·파일·메모 원본과 범위 확정 → 운영자 확인 → 모의 검토 → 학생·등록·자료 종류·관찰시점 확인 → 가져오기 후보 / 제외 / 수동검토로 분리 → 수동검토 해결 → 운영자 승인 → 승인본 재대조 → 학습이력 반영 → 학생별 결과 대사
- **예외·복구·종료:**
  - [ ] 모의 검토: 모의 검토만으로 학생 진도·성적·보고서·포털 공개 상태를 변경하지 않는다.
  - [ ] 승인 후 학생·등록 관계 또는 원본이 바뀜: 실행 중단 → 기존 승인 무효 → 새 모의 검토.
  - [ ] 가져온 진도: 가져온 진도는 G-01 검토로 연결하고 운영자 확인 전 학생·보호자에게 새 확정사실로 게시하지 않는다.
  - [ ] 실행 결과 불명확: 다시 실행하지 않고 실제 반영 항목·제외·미반영 항목을 대사 → 중복 없이 복구 또는 종결.
  - [ ] 이미 완료된 같은 승인본 재요청: 최초 반영 결과로 수렴.
  - 분기·상태 수렴 — 학습 항목 분기: 학생·등록·진도 상태가 명확한 구조화 이력 → 가져오기 후보 / 중복·범위 밖·이미 반영됨 → 제외 / 평가 기준 불명확 → 평가 흐름에서 수동 재구성 또는 제외 / 기존 파일 → 안전한 새 업로드·검사 후 연결 또는 제외 / 자유 메모·학생 불명·등록 불명 → 수동검토 → 새 정상 흐름으로 재작성 또는 제외. 연결 ID: G-01.
- **현행 갭:** ❌ 없음(판정 2026-08-25) — `app/admin/(protected)/students/actions.ts:508` — 학습이력(진도·평가·파일·메모) 가져오기·모의검토·승인·대사 코드 없음 — 존재하는 것은 학생 명부 CSV 일괄 등록(bulkCreateStudents — 이름·연락처 유효성만 필터)뿐. M2·M3(00018·00020)도 이 플로우를 다루지 않았다.

### H-06 비공개 학습파일 업로드·검사·열람
- [ ] **구현 완료** — 업로더 역할·대상·사용목적 확인과 업로드 권한 발급, 파일 제출, 형식·크기·무결성 확인, 비공개 검사 대기, 안전성 결과 확정까지 동작한다.
- **주 전환:** 수업·과제·제출·평가·보고서에서 파일 필요 → 업로더 역할·대상 학생·사용목적 확인 → 업로드 권한 발급 → 파일 제출 → 형식·크기·무결성 확인 → 비공개 검사 대기 → 안전성 결과 확정
- **예외·복구·종료:**
  - [ ] 사용 가능 파일 열람: 로그인·활성 관계·원 기록 권한 재확인 → 제한된 다운로드 → 만료.
  - [ ] 다운로드 권한 만료·생성 실패: 원 파일 상태 유지 → 권한 재확인 후 새 열람 경로.
  - [ ] 관계 종료·과제 철회·보고서 철회: 새 열람 차단 → 보존 필요 확인 → 파일 파기 흐름.
  - [ ] 존재 비노출: 다른 학생의 파일·거절 파일·검사 중 파일은 존재 자체를 노출하지 않는다.
  - [ ] 검사 성공의 한계: 파일 검사 성공이 과제 제출·평가 승인·보고서 게시를 대신하지 않는다.
  - 분기·상태 수렴 — 파일 결과: 안전함 → 사용 가능 → 원 수업·과제·제출·평가·보고서에 연결 / 거절 → 연결·열람 금지 → 사유 안내 → 안전한 파일 재제출 / 검사 지연·시간초과 → 비공개 대기 유지 → 상태 재확인 → 성공/거절/운영자 종결 / 업로드 결과 불명확 → 새 업로드 전 기존 요청 확인 → 이어서 완료/재업로드/취소.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `app/admin/(protected)/materials/actions.ts:50`·`:55` · `app/portal/[token]/actions.ts:43`·`:220`·`:228`·`:523`·`:560` · `scripts/setup-supabase.sh:96` — 관리자 자료 업로드에 더해 포털 과제 제출 파일 업로드(10MB·확장자·MIME 검증, 비공개 homework 버킷, 서명 URL 1시간 만료·본인 제출물만)가 있고, 후기 증빙도 비공개 버킷+서명 URL로 옮겨져(`app/admin/(protected)/reviews/storage.ts:28`·`:119`) 같은 계열 규약이 세 곳으로 늘었다. 남은 갭: 안전성(유해성) 검사 대기·업로더 역할·사용목적별 권한 발급이 여전히 없고, 관계 종료·과제 철회 시 파기 흐름도 없다 — M1이 관계 회수(`lib/portal/auth.ts:524`)를 만들었지만 회수는 세션·링크만 닫고 이미 업로드된 제출 파일에는 손대지 않는다.

### H-07 과제 종료·대상 취소·보관
- [x] **구현 완료** — 게시 과제의 대상별 제출·검토·재제출 상태 확인과 운영자 종료 판단으로 완료·취소·보관에 수렴하는 흐름이 동작한다.
- **주 전환:** 게시 과제 → 대상별 제출·검토·재제출 상태 확인 → 운영자 종료 판단
- **예외·복구·종료:**
  - [ ] 과제 닫힘·보관 후 내용 수정: 기존 과제를 되돌리지 않고 새 과제본 또는 새 과제를 생성.
  - [x] 대상 취소: 대상 취소가 이미 제출된 답안·피드백을 자동 삭제하지 않는다. 공개·보존 범위를 다시 판단.
  - [x] 미검토 제출: 미검토 제출이 남아 있으면 완료·취소·별도 검토 중 하나로 수렴하기 전 전체 종료로 표시하지 않는다.
  - [x] 보관: 보관이 파기를 뜻하지 않으며 권한 있는 과거 학습이력에서는 승인된 범위로 확인 가능.
  - 분기·상태 수렴 — 종료 분기: 대상 완료 → 해당 학생 과제 완료 / 잘못 배정·학습계획 변경 → 해당 대상 취소 → 새 과제 또는 종료 / 전체 과제 종료 → 새 제출 중단 → 진행 중 제출·검토 처리 → 과제 닫힘 / 보존기간·현재업무 종료 → 현재 목록에서 보관 → 이력 접근·파기 정책 유지.
- **현행 갭:** ✅ 있음(판정 2026-08-25, 커밋 4ec60e4) — `app/admin/(protected)/homework/actions.ts:528`·`:582`·`:643` · `supabase/migrations/00015_homework.sql:107`·`:213` · `lib/data/homework.ts:260` — 전체 종료는 미검토 제출 잔존 시 원자 RPC의 WHERE 판정이 거부(검수 126·TOCTOU 제거), 대상 취소는 제출물·피드백 보존(직접 삭제는 DB 트리거 거부)+미검토 잔존분 close_note 기록, 보관(archived_at)은 파기가 아니라 기본 목록 접힘·이력 접근 유지, 닫힘 후 수정은 새 과제본 안내 — E2E 실측 완주(개별 예외 경로 실측은 종료·완결 외 미수행).

## 12. 시험·OMR·AI 채점·성적 분석 (A-01–A-06)

### A-01 평가 준비·배정
- [ ] **구현 완료** — 평가 목적 결정부터 문제·해설·배점·채점기준 검토, 대상·응시조건 연결, 게시·배정, 학생 안내까지 동작한다.
- **주 전환:** 평가 목적 결정 → 문제·해설·배점·채점기준 확인 → 운영자 검토 → 대상·응시조건 연결 → 게시·배정 → 학생 안내
- **예외·복구·종료:**
  - [ ] 자동 인식이 만든 문제·배점 후보: 원문 대조 전 확정 금지.
  - [ ] 준비 미완료: 초안 유지 → 보완 후 다시 검토.
  - [ ] 잘못된 대상 배정: 철회 → 올바른 대상에 재배정.
- **현행 갭:** ❌ 없음(판정 2026-08-25) — `supabase/migrations/00001_init.sql:215` — 평가(문제·해설·배점·채점기준·대상 배정) 엔티티 없음 — grade_records는 exam_name·raw_score·percentile·grade의 사후 결과 수동 기록만. 00016~00020이 성적 소프트 삭제·수업 패키지를 추가했지만 평가 엔티티는 그대로 없다.

### A-02 답안 수집
- [ ] **구현 완료** — 평가 배정 후 학생 온라인 제출 확인과 종이 시험 스캔·일괄 업로드·학생/페이지/문항 분류가 동작한다.
- **주 전환:** 평가 배정 → (분기) 학생 온라인 제출 → 제출 확인 → (분기) 종이 시험 → 운영자 스캔·일괄 업로드 → 학생·페이지·문항 분류
- **예외·복구·종료:**
  - [ ] 일부 파일 실패: 성공 답안 유지 → 실패분만 재업로드.
  - [ ] 중복 답안: 임의 최신 선택 금지 → 실제 응시본 운영자 확인.
  - [ ] 미응시: 0점으로 바꾸지 않고 미응시 상태 유지.
- **현행 갭:** ❌ 없음(판정 2026-08-25) — 관련 코드 없음 — 학생 온라인 제출·종이 시험 스캔·일괄 업로드·학생/페이지/문항 분류 어느 것도 없음. 포털 제출은 과제(H-02)뿐이고 평가 답안 경로는 없다.

### A-03 학생·페이지 매칭 예외
- [ ] **구현 완료** — 자동 분류 후 학생번호·페이지·문항 수 확인으로 정상·미매칭·중복·보완 스캔·해결 불가 처리가 동작한다.
- **주 전환:** 자동 분류 → 학생번호·페이지·문항 수 확인
- **예외·복구·종료:**
  - [ ] 정상: 채점 대기.
  - [ ] 학생 식별 실패: 미매칭 큐 → 운영자가 학생 연결.
  - [ ] 중복 학생번호: 두 답안 모두 보존 → 실제 학생·응시본 확인.
  - [ ] 페이지 누락·순서 오류·품질 부족: 보완 스캔 요청 → 새 파일 연결.
  - [ ] 해결 불가: 수동 채점 또는 미완료 종료, 정상 답안 처리는 유지.
- **현행 갭:** ❌ 없음(판정 2026-08-25) — 관련 코드 없음 — 자동 분류·미매칭 큐·중복 학생번호 처리 등 답안 매칭 파이프라인 부재.

### A-04 자동·AI 채점과 사람 승인
- [ ] **구현 완료** — 확정 답안·정답·채점기준 기반 자동·AI 채점 초안과 불확실 항목 분리, 운영자 원문 대조·수정·전체 검토·승인까지 동작한다.
- **주 전환:** 확정 답안·정답·채점기준 → 자동·AI 채점 초안 → 불확실 항목 분리 → 운영자 원문 대조 → 수정 → 전체 검토 완료 → 승인
- **예외·복구·종료:**
  - [ ] AI·채점 제공자 실패: 동일 답안을 수동 채점 흐름으로 전환.
  - [ ] 기준 불일치: 채점 중단 → 기준 정정 → 영향 답안 전부 재채점.
  - [ ] 승인 전: 점수·첨삭·통계 비노출.
  - [ ] 승인 후 오류: 원 결과 유지 → 정정본 → 재검토·재승인.
- **현행 갭:** ❌ 없음(판정 2026-08-25) — `lib/ai/generate.ts:81` — 자동·AI 채점 코드 없음 — lib/ai/generateReport는 리포트 문안 생성 전용이며 답안 채점·불확실 항목 분리·재채점 흐름이 아님. AI 호출부 4곳(성적·상담·수업·리포트) 어디에도 채점은 없다.

### A-05 분석·성적표·보완 학습
- [ ] **구현 완료** — 승인 결과의 학생별·문항별·단원별 집계와 비교 가능성 확인, 운영자 검토, 성적표 승인·게시, 학생·보호자 알림까지 동작한다.
- **주 전환:** 승인 결과 → 학생별·문항별·단원별 집계 → 비교 가능성 확인 → 운영자 검토 → 성적표 승인·게시 → 학생·보호자 알림
- **예외·복구·종료:**
  - [ ] 결측·미응시: 0점으로 대체하지 않는다.
  - [ ] 비교 기준 불일치: 순위·변화 대신 해당 평가의 사실만 표시.
  - [ ] 취약 문항: 재풀이 과제·질문·다음 수업계획으로 연결.
  - [ ] 진도·목표 변경: 학습목표 흐름으로 연결.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `app/admin/(protected)/grades/actions.ts:286`·`:300` · `app/admin/(protected)/grades/page.tsx:19` · `app/admin/(protected)/reports/actions.ts:366` · `lib/data/crm.ts:195` — 수동 성적 기록 기반 시험 리포트 AI 초안(이전 시험 시간순 추이 포함)→승인→포털 게시·알림은 그대로 동작하고, 00016이 붙으면서 철회(소프트 삭제)된 성적은 집계·추이·리포트 근거에서 제외된다(`lib/data/crm.ts:437`·`:814`) — 남은 갭: 문항별·단원별 집계, 결측·미응시 0점 방지 규칙, 보완 학습(재풀이 과제·학습목표) 연결이 여전히 없다. A-01~A-04가 없어 '승인 결과'라는 입력 자체가 수동 입력 성적이다.

### A-06 평가 정정·재시험·재사용
- [ ] **구현 완료** — 정정 요청·재시험 결정 후 원 평가·기준 확인을 거쳐 새 결과본·새 평가본으로 분리해 승인·재게시까지 동작한다.
- **주 전환:** 정정 요청 또는 재시험 결정 → 원 평가·기준 확인
- **예외·복구·종료:**
  - [ ] 정정: 새 결과본 → 승인 → 성적표·보고서 영향 갱신 → 재게시.
  - [ ] 재시험: 새 응시 결과 생성 → 답안 수집부터 다시 진행.
  - [ ] 평가 재사용: 문제·기준만 복제하고 기존 학생 답안·점수는 복제하지 않는다.
  - [ ] 기준 변경: 새 평가본으로 분리해 어떤 기준이 적용됐는지 연결.
  - [ ] 평가 철회: 새 응시·공개 중단 → 이미 게시된 학생 결과·보고서 영향 확인 → 결과 유지/철회/정정 결정 → 대상자 안내.
  - [ ] 결과 철회 후 다시 공개: 이전 결과를 되돌리지 않고 정정된 새 결과본을 검토·승인·게시.
- **현행 갭:** ⚠️ 충돌(정본 우선)(판정 2026-08-25) — 두 충돌 중 **물리 삭제는 해소, 덮어쓰기는 잔존**이다. 해소분: `supabase/migrations/00016_tier1_conflicts.sql:26`(grade_records.deleted_at·deleted_reason) · `app/admin/(protected)/grades/actions.ts:207`·`:265`·`:268`(deleteGrade가 DELETE 대신 deleted_at 스탬프 · 사유 필수 · 이미 철회된 행은 덮어쓰지 않는 WHERE 가드) · `lib/data/crm.ts:437`·`:459`·`:814`(활성 행만 조회 — 목록·상세·리포트 근거에서 철회분 제외) · `app/admin/(protected)/grades/[id]/page.tsx:22`·`:131`(철회 이력 표시·사유 입력) — 스키마만이 아니라 호출부까지 채택됐다. 잔존분: `app/admin/(protected)/grades/actions.ts:108`·`:176`(updateGrade가 여전히 grade_records 원 행을 in-place UPDATE로 덮어쓴다) — 전후 값·사유·행위자는 append-only 조정 이력(`lib/data/adjustments.ts:87` recordAdjustment, 기록 실패 시 정정 자체를 실행하지 않는 fail-closed)에 남지만, 정본이 요구하는 '원 결과 유지 → 새 결과본 → 재승인 → 재게시'의 **새 결과본이 생기지 않는다**. 남은 갭: 재시험·평가 재사용 개념 없음(A-01 부재), '성적표·보고서 영향 갱신'은 성적 정정에서 자동으로 열리지 않는다 — report_recheck 업무는 출결 정정 경로에만 붙어 있다(`supabase/migrations/00020_lesson_packages.sql:982`).

## 13. 진도·학습목표·학습보고서 (G-01–G-03)

### G-01 진도·학습목표
- [ ] **구현 완료** — 수업·과제·평가·승인된 기존 학습이력 근거 확인부터 단원 상태 갱신, 목표 판단, 학습계획 제안, 운영자 검토까지 동작한다.
- **주 전환:** 수업·과제·평가·승인된 기존 학습이력 근거 확인 → 단원 상태 갱신 → 목표 유지·달성·보완·종료 판단 → 학습계획 제안 → 운영자 검토
- **예외·복구·종료:**
  - [ ] 근거 부족: 추정하지 않고 확인 필요.
  - [ ] 과거 상태 변경: 기존 기록 유지 → 새 상태 기록.
  - [ ] 전체 목표 달성: 구성 항목이 모두 충족된 경우에만 종료.
  - [ ] 활성 목표 일시중단: 중단 사유·재검토 조건 확인 → 일시정지 → 재개 시 최신 근거로 다시 검토.
  - [ ] 목표 달성·취소: 종료 상태로 유지하고 다시 필요하면 새 목표를 생성해 이전 목표와 연결.
  - [ ] 제안 상태: 제안만 생성된 상태를 실제 적용된 학습계획으로 게시하지 않는다.
  - [ ] 적용 후 근거 정정: 원 계획 유지 → 영향 확인 → 새 제안으로 수정·대체.
  - 분기·상태 수렴 — 학습계획 제안: 확인 → 적용대기 → 수업·과제·평가 계획에 연결 → 적용완료 / 기각 → 현재 계획 유지 → 제안 종료 / 근거 보완 → 제안 유지 → 근거 추가 후 재검토 / 새 제안으로 대체 → 이전 제안 종료 → 새 제안 검토.
- **현행 갭:** ❌ 없음(판정 2026-08-25) — `supabase/migrations/00001_init.sql:160` — 학습목표·단원 상태·학습계획 제안 엔티티/코드 없음 — lessons는 회차·내용·숙제·집중도·태도 기록뿐이고 '진도/목표/단원' 검색 결과는 공개 사이트 마케팅 문구(`lib/defaults.ts:293`·`:341`)만. M3(00020)가 수업 회차·출결·잔액 원장을 채웠지만 그것은 **일정과 잔액**이지 단원 상태·목표 판단이 아니다 — 근거(수업·과제)는 쌓이는데 그 근거를 읽어 단원 상태를 갱신하는 층이 없다.

### G-02 학습보고서 작성·게시
- [x] **구현 완료** — 보고 기간 확정과 수업·출결·과제·평가·진도 근거 수집, 초안 작성, 근거 대조, 운영자 수정·승인, 대상 확인, 게시·알림까지 동작한다.
- **주 전환:** 보고 기간 확정 → 수업·출결·과제·평가·진도 근거 수집 → 사람 작성 또는 AI 초안 → 근거 대조 → 운영자 수정·승인 → 학생·보호자 대상 확인 → 게시 → 알림
- **예외·복구·종료:**
  - [ ] 근거 없음: 만들어내지 않고 기록 없음으로 유지.
  - [ ] AI 실패: 사람 작성 흐름으로 전환.
  - [x] 승인 전: 포털·알림·공유 비노출.
  - [ ] 게시 실패: 승인본 유지 → 게시 재시도.
  - [x] 알림 실패: 보고서 게시 유지 → 전달 재시도.
- **현행 갭:** ✅ 있음(판정 2026-08-25) — `app/admin/(protected)/reports/actions.ts:184`·`:234`·`:239`·`:317`·`:366`·`:410`·`:445`·`:487` · `lib/data/reports.ts:139` · `lib/data/crm.ts:195` · `app/portal/[token]/page.tsx:66` · `lib/portal/data.ts:103`·`:117` · `lib/ai/validate.ts:105` · `lib/notify/send.ts:245` · `app/api/cron/flush/route.ts:36` — 근거 수집(수업·성적)→가명화 AI 초안(항상 draft)→운영자 수정(발송 후 금지)·승인(draft만)→발송 전 클린 검사→포털 게시(approved/sent만 노출·실명 복원)→알림(업무 상태와 전달 상태 delivery_status 분리 — 실패가 게시를 뒤집지 않음, 큐 크론 재시도)까지 생성→승인→포털 실명 복원 E2E 실측. 00016이 status 체크에 retracted를 더하면서도 전달 상태 'failed'가 업무 상태로 되돌아오지 않는 분리는 그대로임을 RLS 하네스 8m-b(`supabase/tests/rls/99_rls_test.sql:833`)로 실증 — 남은 갭: 사람 직접 작성 경로·보고 기간 지정 없음, 근거 없는 항목을 '기록 없음'으로 강제하는 규칙은 문안 검증 수준이지 근거 대조 단계가 아니다.

### G-03 보고서 정정·철회·공유
- [ ] **구현 완료** — 오류·정정 요청부터 원 보고서·근거 확인, 새 보고서본, 운영자 재승인, 최신본 게시, 이전본 대체 표시까지 동작한다.
- **주 전환:** 오류·정정 요청 → 원 보고서와 근거 확인 → 새 보고서본 → 운영자 재승인 → 최신본 게시 → 이전본 대체 표시
- **예외·복구·종료:**
  - [x] 철회: 새 열람 차단 → 철회 이력 보존.
  - [ ] 지정 수신자 공유: 관계·대상·보고서본 확인 → 제한된 공유 → 본인 확인 → 열람.
  - [ ] 공유 만료·철회·새 보고서본 게시: 기존 공유 경로 종료.
  - [ ] 오수신: 즉시 공유 회수 → 접근 여부 확인 → 보안사고 흐름.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — **정정·철회·대체 표시 충돌은 해소**됐다: `supabase/migrations/00016_tier1_conflicts.sql:40`·`:44`·`:59`(retracted_at·retract_reason·superseded_by 자기참조 복합 FK·status에 retracted 추가) · `app/admin/(protected)/reports/actions.ts:207`(정정본 생성 시 이전 본과 학생·유형·대상 일치 검증, 초안·이미 대체된 본은 대체 대상에서 제외)·`:283`(새 초안 생성과 이전 본 철회·연결을 한 감사 사건으로 묶고, 연결 실패 시 방금 만든 초안을 회수)·`:373`(draft만 승인 — 철회본 재승인 금지)·`:419`(철회본 발송 금지)·`:566`(단독 철회 — 사유 필수·게시본만 대상)·`:72`(철회 순서 계약: 대기·재시도 알림을 먼저 회수한 뒤 상태 전환) · `lib/data/crm.ts:195`·`lib/portal/data.ts:78`(포털은 approved·sent 화이트리스트라 retracted가 자동 제외 = 새 열람 차단) · `app/admin/(protected)/reports/[id]/page.tsx:76`·`:192`(대체 링크·철회 시각·사유 표시) — 스키마·호출부·노출 경로가 모두 붙었고, retracted 허용·타테넌트 대체 연결 차단은 RLS 하네스 8m-a·c·d(`supabase/tests/rls/99_rls_test.sql:818`)로 실증. 남은 갭(공유 쪽): M1이 관계별 초대 링크·서버측 세션·즉시 회수를 만들어(`supabase/migrations/00017_role_portal.sql:64`·`:113` · `lib/portal/auth.ts:444` rotateAccessLink · `:524` revokeRelation) 지정 수신자 공유·회수의 뼈대는 생겼지만, ① 링크에 만료가 없어 '공유 만료'가 회수로만 성립하고 ② 새 보고서본 게시가 기존 공유 경로를 종료시키지 않으며(같은 포털 목록에서 최신본으로 갈릴 뿐) ③ 오수신 시 '접근 여부 확인 → 보안사고 흐름'이 W-04 부재로 이어지지 않는다. 구 경로 `/portal/[token]`(학생당 영구 토큰 하나)도 병행 운영 중이라 본인 확인 없는 공유 표면이 남아 있다.
