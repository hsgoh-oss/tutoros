# Atlas 체크리스트 — 유입 — 개통·상담·시범·정규등록 (O·C·T·R)

> 정본: TutorOS_Visual_Flow_Atlas.docx (SHA-256 03BC551C…8301) · 체크 = 해당 흐름이 코드로 실제 동작함(구현 완료) · 갭: ✅ 있음 / 🔶 부분 / ❌ 없음 / ⚠️ 충돌(정본 우선)

## 5. 서비스 개통·공개 운영 (O-01–O-05)

### O-01 최초 운영 개통
- [ ] **구현 완료** — 공개 정보와 실제 접수 가능 상태가 일치한다.
- **주 전환:** 운영자 본인·강화 인증 확인 → 운영에 필요한 정책·수업 조건·연락 경로 확인 → 결제·메시지·AI·파일 제공자 상태 확인 → 백업 확인 → 격리 복원 검증 확인 → 공개 내용 검토 → 운영자 개통 승인 → 상담 접수 개시
- **예외·복구·종료:**
  - [x] 필수 운영조건 미완료: 해당 외부 흐름만 닫고 가짜 성공을 만들지 않는다.
  - [ ] 백업·복원·보안처럼 전체 운영을 막아야 하는 조건 미완료: 개통 보류 → 원인별 운영 업무 → 재검사.
  - [x] 상담 접수가 닫힌 상태: 방문자를 대기 신청 또는 운영 재개 안내로 보낸다.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `lib/actions/consult.ts:55`, `app/(public)/consult/page.tsx:24`, `middleware.ts:16`, `supabase/migrations/00001_init.sql:51`, `lib/data/backup.ts:31`, `lib/notify/send.ts:112` — M2로 '상담 접수 개시·중단' 축만 생겼다: 모집 상태가 closed면 공개 상담 페이지가 폼 대신 마감 안내를 띄우고 서버 액션도 접수를 거부해 화면 우회로 접수되지 않는다. 관리자 OTP 인증·직전본 백업·제공자 미설정 시 queued 적재는 그대로다. 없음: 개통이라는 사건 자체(레포 전역 '개통' 무검출) — 필수 운영조건 점검, 격리 복원 검증, 운영자 개통 승인, 개통 보류 후 원인별 업무·재검사 경로가 전부 없다.

### O-02 공개 내용 게시·수정·철회
- [ ] **구현 완료** — 승인된 내용만 게시되고 공개 확인 후 변경·만료·철회가 감시된다.
- **주 전환:** 초안 → 사실·출처·공개기한 확인 → 운영자 검토 → 승인 → 즉시 또는 예약 게시 → 공개 확인 → 변경·만료·철회 감시
- **예외·복구·종료:**
  - [ ] 오류 발견: 공개 중단 → 정정본 작성 → 재검토 → 재게시.
  - [ ] 예약 실패: 승인본을 유지한 채 재시도 업무 생성.
  - [ ] 게시기한 도달: 공개 종료 → 검색·공유 경로 종료 → 만료 이력 보존.
  - [ ] 원 출처의 변경·취소: 현재 공개본을 사실과 대조 → 필요하면 즉시 숨김 → 정정본 승인 후 재게시.
  - [ ] 공개 주소 변경: 새 승인본 게시 → 이전 주소에서 현재 주소로 안전하게 연결 → 반복·잘못된 대상 확인 → 이전 주소 은퇴 시 연결 종료.
  - [ ] 철회: 공개·검색·공유 경로를 함께 닫고 철회 이력을 보존.
- **현행 갭:** 🔶 부분(판정 2026-08-25 — 변화 없음, 줄 번호만 갱신) — `app/admin/(protected)/settings/actions.ts:29`, `app/admin/(protected)/settings/actions.ts:123`, `app/admin/(protected)/faq/actions.ts:41`, `app/admin/(protected)/faq/actions.ts:234`, `lib/data/backup.ts:31` — 저장 즉시 게시(revalidatePath)와 직전본 백업·복원만 있고 초안→검토→승인, 예약 게시, 게시기한·만료, 주소 이전, 철회 이력 흐름 없음(page_contents 테이블은 여전히 미사용 — 앱 전역 참조 0건).

### O-03 운영자 부재
- [ ] **구현 완료** — 복귀 후 누적 업무를 확인하고 정상 운영으로 복귀한다.
- **주 전환:** 부재 예정 등록 → 공개 안내 → 신규 상담은 계속 접수 → 긴급·기한 업무 분리 → 복귀 → 누적 업무 확인 → 정상 운영
- **예외·복구·종료:**
  - [ ] 부재 중 자동 취소 금지: 부재가 수업·결제·환불을 자동 취소하지 않는다.
  - [ ] 처리 불가능한 회차: 처리 불가능한 회차는 개별 변경·취소 플로우로 보낸다.
- **현행 갭:** ❌ 없음(판정 2026-08-25 — 변화 없음) — 관련 코드 없음 — 부재 예정 등록·공개 안내·긴급 업무 분리·복귀 흐름을 앱/lib/컴포넌트 전역에서 찾을 수 없음(부재·휴가 키워드 무검출). M2는 유입 퍼널만 건드려 이 도메인에 닿지 않았다.

### O-04 모집 정원·접수 상태 운영
- [x] **구현 완료** — 모집 가능 / 대기만 가능 / 접수 중단 중 하나로 공개 상태가 확정된다.
- **주 전환:** 현재 활성 등록·확정 제안·보류 자리 확인 → 실제 수용 가능 인원 확인 → 운영자 결정 → 모집 가능 / 대기만 가능 / 접수 중단 중 하나로 공개 상태 확정
- **예외·복구·종료:**
  - [x] 자리 증가: 대기명단 순서와 현재 조건 확인 → 한 자리마다 한 사람에게만 제안.
  - [x] 자리 감소: 이미 확정된 등록·유효한 자리 제안을 자동 취소하지 않음 → 새 제안 중단 → 운영자 조정.
  - [x] 예약된 자리보다 적은 정원으로 변경 시도: 변경 보류 → 열린 제안·등록 확인 → 다시 결정.
  - [ ] 공개 상태 변경 실패: 내부 정원 상태를 유지하고 공개 내용 불일치 업무 생성 → 일치 확인 전 신규 제안 중단.
  - [ ] 모집 재개: 정원과 대기명단을 다시 확인한 뒤 공개 접수와 자리 제안을 각각 재개.
- **현행 갭:** ✅ 있음(판정 2026-08-25 · ⚠️ 충돌 해소) — `lib/actions/consult.ts:50`·`:55`·`:63`, `app/(public)/consult/page.tsx:24`, `lib/data/intake.ts:836`, `app/admin/(protected)/recruit/actions.ts:89`·`:132`·`:247`, `supabase/migrations/00018_intake_funnel.sql:362` — 공개 상태가 실제 접수 가능 상태와 묶였다: closed면 폼과 서버 액션이 함께 접수를 거부하고, waitlist면 접수는 받되 상담을 'hold'(대기 신청)로 구분 적재해 모집 화면의 대기자 목록이 된다. 수용 인원은 getSeatAvailability가 활성 등록 + 준비 중 등록 + 살아 있는 자리 제안을 상담 기준 중복 제거해 산정하고, 예약된 자리보다 적은 정원으로 줄이려 하면 저장을 멈춘 뒤 '축소 강행' 확인을 요구한다(강행해도 기존 등록·제안은 취소하지 않고 초과 사실만 업무로 남긴다). 새 제안은 남은 자리 게이트가 막고, 한 자리에 한 사람은 부분 유니크가 강제한다. 남은 갭: 공개 상태 저장이 실패해도 '공개 내용 불일치 업무 생성 + 신규 제안 중단'이 없어 오류 문구만 돌아가고, 모집 재개 시 정원·대기명단을 다시 확인하게 하는 절차도 별도로 없다.

### O-05 공개 콘텐츠 이미지·미디어 처리
- [ ] **구현 완료** — 미디어가 사용 가능 또는 거절로 확정되고 승인 콘텐츠본에만 연결된다.
- **주 전환:** 공개 콘텐츠 초안에서 미디어 필요 확인 → 대상·사용목적 확인 → 파일 제출 → 임시 대기 → 실제 형식·무결성·안전성 확인 → 비공개 격리 → 안전한 변환·개인정보 흔적 제거 → 운영 사용 가능 또는 거절 확정 → 분기(미디어 결과): 사용 가능 → 해당 승인 콘텐츠본에 연결 → 콘텐츠 승인·게시 → 분기(미디어 결과): 거절 → 공개·연결 금지 → 대체 파일 요청 또는 미디어 없이 재검토 → 분기(미디어 결과): 검사·처리 실패 → 비공개 대기 → 재시도 → 성공/거절/운영자 종결 → 분기(미디어 결과): 교체·철회 → 새 연결 차단 → 사용 중인 공개본 확인 → 삭제대기 → 잔존 확인 → 삭제완료
- **예외·복구·종료:**
  - [ ] 사용 가능 전 미디어: 사용 가능 전 미디어를 예약 게시·공개 미리보기·공유에 포함하지 않는다.
  - [ ] 다른 활성 공개본이 같은 미디어 사용 중: 다른 활성 공개본이 같은 미디어를 사용 중이면 먼저 연결을 정리하고 삭제 여부를 다시 판단한다.
  - [ ] 새 미디어 교체: 새 파일을 처음부터 검사 → 새 콘텐츠본 승인 → 공개 전환 → 이전 파일 삭제대기.
  - [ ] 같은 원본의 공개용 변형본: 안전 처리 완료 후 사용 가능 → 콘텐츠 연결 종료 시 함께 은퇴·삭제 검토.
  - [ ] 삭제 실패: 공개 경로는 닫은 채 재시도·잔존 확인, 완료 전 삭제완료로 표시하지 않는다.
  - [ ] 공개 미디어에 학생·보호자 개인정보 필요: 공개 미디어에 학생·보호자 개인정보가 필요하면 별도 동의·최소공개·철회 흐름을 함께 적용한다.
- **현행 갭:** 🔶 부분(판정 2026-08-25 — 변화 없음) — `app/admin/(protected)/materials/actions.ts:15`, `app/admin/(protected)/materials/actions.ts:55`, `app/admin/(protected)/reviews/actions.ts:38`, `app/admin/(protected)/reviews/actions.ts:288`, `lib/images.ts:12` — 확장자·MIME·크기 검증, 랜덤 파일명, 비공개 버킷(materials)·서명 URL, 후기 승인 시점의 공개 사본 생성(publishEvidenceCopies)은 있으나 격리→안전성 검사→변환→개인정보 제거→삭제대기 수명주기 없음(toWebp는 정의만 있고 호출부 없음). M2는 이 도메인에 닿지 않았다.

## 6. 상담신청·상담·대기 (C-01–C-07)

### C-01 상담신청서 접수
- [ ] **구현 완료** — 접수번호 1개와 운영자 상담 업무 1개.
- **주 전환:** 공개 안내 확인 → 신청 주체·연령 경로 확인 → 최소 상담정보 작성 → 개인정보 동의 → 제출 → 중복 방지 처리 → 접수번호 발급 → 접수 완료 → 운영자 신규 상담 업무
- **예외·복구·종료:**
  - [x] 미성년자 본인 신청이 허용되지 않는 연령: 개인정보 입력 전 보호자 신청 경로로 전환.
  - [x] 입력 오류: 저장하지 않고 같은 작성 흐름에서 수정.
  - [ ] 중복 클릭·재전송: 최초 접수 결과로 수렴.
  - [ ] 저장 결과 불명확: 새 접수를 만들지 않고 기존 요청 결과 확인.
  - [ ] 알림 실패: 상담은 접수 완료로 유지하고 전달 실패 업무 생성.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `lib/actions/consult.ts:30`, `lib/actions/consult.ts:55`, `lib/actions/consult.ts:86`, `lib/actions/consult.ts:123`, `lib/actions/consult.ts:127`, `components/public/consult/schema.ts:80`, `supabase/migrations/00001_init.sql:285` — 접수·필수 동의(consents)·만14세 보호자 경로(superRefine 서버 재검증)·동의 실패 시 보상 삭제·고객/관리자 알림·알림 실패에도 접수 유지는 그대로이고, M2로 모집 마감 시 접수 거부와 대기 신청(status='hold') 구분 적재가 붙었다. 없음: 접수번호 발급(레포 전역 '접수번호' 무검출)과 중복 클릭·재전송의 최초 접수 수렴 — 멱등 키가 없어 같은 내용을 두 번 제출하면 상담 행이 두 건 만들어진다.

### C-02 중복·기존 관계 확인
- [ ] **구현 완료** — 기존 건 연결 / 별도 건 / 중복 종결 중 하나.
- **주 전환:** 신규 접수 → 동일 연락처·학생·열린 상담 후보 확인 → 운영자 판단
- **예외·복구·종료:**
  - [ ] 같은 상담의 재제출: 기존 상담에 연결.
  - [ ] 같은 학생의 새 상담: 기존 학생·관계에 연결하되 새 상담 이력은 유지.
  - [ ] 형제자매: 기존 보호자를 공유하고 학생은 별도 연결.
  - [ ] 동일 연락처지만 다른 사람: 별도 접수 유지.
  - [ ] 판단 전: 다음 단계 폼 발송과 등록 전환을 보류.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `app/admin/(protected)/consultations/actions.ts:784`, `app/admin/(protected)/consultations/intake-constants.ts:50`, `app/admin/(protected)/consultations/intake-constants.ts:57`, `app/admin/(protected)/consultations/actions.ts:189`, `lib/actions/consult.ts:70` — 예외 「판단 전: 다음 단계 폼 발송 보류」만 서버 게이트로 생겼다: 운영자가 중복·기존 관계 확인을 선언하지 않으면 시범·정규 신청폼 발급이 거부되고, 그 선언이 감사 사유로 남는다(화면 체크박스만이 아니라 액션 층에서 막는다). 없음: 후보를 찾아주는 대조 로직 — 동일 연락처·기존 학생·열린 상담을 조회하는 코드가 없어 판단 재료는 여전히 운영자 기억이고, 확인 사실을 저장할 컬럼이 없어 선언은 발급 1회에만 유효하다. 등록 생성(createEnrollment)·상담→학생 전환에는 같은 게이트가 걸리지 않고, 형제자매·재제출을 기존 관계에 연결하는 경로도 없다('이미 학생으로 전환된 상담' 가드 한 줄이 전부).

### C-03 신청 상태 조회·접수번호 복구
- [ ] **구현 완료** — 본인 상담 상태 표시 또는 안전한 실패 종료.
- **주 전환:** 접수번호·본인 확인 → 제한된 진행 상태 확인 → 다음 행동 확인
- **예외·복구·종료:**
  - [ ] 접수번호 분실: 존재 여부를 노출하지 않는 본인 확인 → 1회성 복구 → 상태 조회.
  - [ ] 본인 확인 실패·타인 신청: 동일한 닫힌 결과.
  - [ ] 조회 권한 만료: 상담 데이터는 바꾸지 않고 조회 권한만 종료.
- **현행 갭:** ❌ 없음(판정 2026-08-25 — 변화 없음) — `lib/actions/consult.ts:26` — 관련 코드 없음 — 제출 결과는 ok/에러뿐, 접수번호 자체가 없어 상태 조회·본인 확인·1회성 복구 공개 라우트가 존재하지 않음. M2가 만든 공개 라우트는 신청폼 작성(`/f/[token]`) 하나이고 상태 조회용이 아니다.

### C-04 접수 검토·첫 연락
- [ ] **구현 완료** — 첫 연락 결과가 기록되고 상담 일정 또는 후속 연락으로 이어진다.
- **주 전환:** 신규 상담 업무 → 중복·대상·정원·오입력 확인 → 필요한 보완 요청 → 선호 경로로 첫 연락 → 연락 결과 기록 → 상담 일정 또는 후속 연락
- **예외·복구·종료:**
  - [ ] 민감정보 오입력: 다른 메모로 복제하지 않고 격리·삭제 흐름으로 인계.
  - [ ] 연락 성공: 상담 진행.
  - [ ] 연락 실패: 다음 연락일 지정.
  - [ ] 반복 무응답: 운영자 최종 확인 후 종결.
  - [ ] 신청자 재연락: 종결 전이면 상담으로 복귀, 종결 후면 새 상담 생성.
- **현행 갭:** 🔶 부분(판정 2026-08-25 — 변화 없음, 줄 번호만 갱신) — `app/admin/(protected)/consultations/actions.ts:39`, `app/admin/(protected)/consultations/actions.ts:74`, `app/admin/(protected)/consultations/actions.ts:112`, `app/admin/(protected)/consultations/constants.ts:5` — 상태(신규→연락) 변경·메모·수동 안내 발송·activity_log만 존재. 정원 확인 재료는 M2 모집 화면(getSeatAvailability)에서 볼 수 있게 됐지만 첫 연락 흐름과는 이어지지 않는다. 없음: 보완 요청, 다음 연락일 지정, 반복 무응답 종결(상담 상태값에 종결이 없다), 민감정보 오입력 격리·삭제 인계.

### C-05 상담 결과
- [ ] **구현 완료** — 운영자가 다음 결과 하나를 선택해 해당 다음 단계로 연결된다.
- **주 전환:** 학습 상태·목표·방식·일정 가능성 확인 → 운영자가 다음 결과 하나 선택 → 분기(상담 결과): 시범수업 진행 → 시범수업 신청폼 → 분기(상담 결과): 시범 생략 정규 제안 → 정규수업 신청폼 → 분기(상담 결과): 정원 대기 → 대기명단 → 분기(상담 결과): 추가 확인 → 후속 상담일 → 상담 결과로 복귀 → 분기(상담 결과): 부적합·철회·무응답·미진행 → 상담 종결
- **예외·복구·종료:**
  - [x] 결과 변경: 기존 다음 단계 링크를 먼저 닫고 새 결과를 생성.
  - [ ] 동시 활성화 제한: 동시에 활성화되는 다음 단계는 하나.
  - [ ] 종결: 열린 폼과 자리 제안을 모두 닫고 보존기한을 시작.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `app/admin/(protected)/consultations/actions.ts:774`, `app/admin/(protected)/consultations/actions.ts:613`, `app/admin/(protected)/consultations/actions.ts:654`, `app/admin/(protected)/consultations/actions.ts:876`, `lib/data/intake.ts:934`, `supabase/migrations/00018_intake_funnel.sql:88` — 다섯 분기 중 셋이 실제 링크가 됐다: 시범 신청폼 발급, 정규 신청폼 발급, 대기 신청(hold)과 자리 제안. 결과를 바꾸면 이 상담의 열린 폼을 종류 불문 먼저 닫고(같은 종류 재발급은 부분 유니크가 DB에서 막는다) 열린 자리 제안은 그 사람의 자리로 확정하며, 상담 상세는 지금 열린 갈래 수를 세어 '동시 활성 하나'가 깨졌는지 보여준다. 없음: 추가 확인 분기의 후속 상담일 지정과 부적합·철회·무응답·미진행의 상담 종결 — 상담 상태값에 종결이 없어 두 분기는 갈 곳이 없고, 종결 시 보존기한 시작도 없다.

### C-06 대기명단
- [ ] **구현 완료** — 자리 제안이 수락·거절·만료·재협의·철회 중 하나로 종료되고 자리 상태가 정리된다.
- **주 전환:** 정원 부족 또는 대기 접수 선택 → 대기 동의 → 희망조건·대기 시작점 연결 → 대기 → 자리 발생 → 정원·열린 제안·대기 순서 재확인 → 운영자 적합성 확인 → 한 명에게 승인된 기간의 자리 제안 → 분기(자리 제안): 수락 → 자리 예약 → 시범 또는 정규 제안 → 해당 폼 발송 → 분기(자리 제안): 거절 → 제안 종료 → 자리 반환 → 다음 대기자 검토 → 분기(자리 제안): 기간 만료 → 제안 종료 → 자리 반환 → 다음 대기자 검토 → 분기(자리 제안): 고객 조건 변경 → 제안 종료 또는 재협의 → 대기조건·순서 재판단 → 분기(자리 제안): 운영자 철회 → 사유 확인 → 자리 반환 → 대기 또는 종결
- **예외·복구·종료:**
  - [ ] 대기 철회: 열린 자리 제안 닫기 → 종결·보존 흐름.
  - [ ] 제안 수락 후 다음 단계 폼 미제출·만료: 자리 예약 해제 여부를 운영자가 확인 → 대기 복귀 또는 종결.
  - [x] 자리 제안 중 정원 감소: 기존 제안을 자동 취소하지 않고 운영자 조정 전 새 제안을 중단.
  - [x] 동시 제안 금지: 같은 자리를 여러 사람에게 동시에 제안하거나 확정하지 않는다.
  - [x] 대기순서 자동 확정 금지: 대기순서만으로 자동 확정하지 않으며 현재 조건·관계·진행 가능성을 운영자가 다시 확인한다.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `supabase/migrations/00018_intake_funnel.sql:338`, `supabase/migrations/00018_intake_funnel.sql:362`, `supabase/migrations/00018_intake_funnel.sql:491`, `app/admin/(protected)/recruit/actions.ts:247`, `app/admin/(protected)/recruit/actions.ts:516`, `lib/data/intake.ts:775`, `lib/actions/consult.ts:63` — 기간부 자리 제안이 생겼다: 대기(hold) 상담에만 1~30일 회신 기한으로 제안하고, 한 자리에 살아 있는 제안·수락은 하나뿐(부분 유니크 + RPC가 경합을 같은 판정으로 수렴), 수락은 자리 예약이고 거절·만료는 자리를 반환한다. 대기 순서만으로 자동 확정하거나 다음 대기자에게 자동 승계하는 코드는 의도적으로 두지 않았다(한 자리 동시 제안 차단·자리 반환은 하네스 8v 절 `supabase/tests/rls/99_rls_test.sql:1782`의 검사 대상). 없음: 대기명단 자체의 엔티티 — 대기 동의·희망조건·대기 시작점·순번이 별도로 없어 상담 행과 접수 시각이 대신하고, 고객 조건 변경에 따른 재협의와 대기 철회 경로도 없다. 제안 수락 후 다음 단계 폼이 미제출·만료돼도 예약된 자리를 다시 판단하는 흐름이 없어 accepted 자리는 그대로 묶인 채 남고, 기한 경과분의 자동 만료도 크론 없이 운영자 확정 또는 다음 제안 시 lazy 정리로만 일어난다.

### C-07 상담 철회·종결
- [ ] **구현 완료** — 다음 행동이 없는 상담 종결 또는 정산·파기 흐름으로 명확히 인계.
- **주 전환:** 철회 또는 종결 사유 확인 → 열려 있는 폼·제안 닫기 → 미수·결제가 있으면 정산 흐름 → 고객에게 종료 안내 → 보존기한 시작
- **예외·복구·종료:**
  - [ ] 이미 등록 활성화: 상담 종결이 아니라 등록 종료 흐름으로 전환.
  - [ ] 결제 결과 불명확: 종결 완료 전에 결제 대사.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `app/admin/(protected)/consultations/actions.ts:876`, `app/admin/(protected)/recruit/actions.ts:521`, `lib/data/intake.ts:934`, `supabase/migrations/00001_init.sql:273` — 「열려 있는 폼·제안 닫기」만 개별 액션으로 생겼다: 신청폼은 사유를 받아 닫히고(제출된 폼은 덮어쓰지 않는다) 자리 제안은 거절로 닫혀 자리를 반환하며, 상담에 남은 열린 폼·회차·등록·제안을 한 번에 확인할 수 있다. 없음: 종결 자체를 기록할 자리 — 상담 상태값이 그대로(new/contacted/trial/registered/hold)라 철회·종결 사유를 남길 컬럼이 없고, 미수·결제 정산 인계, 고객 종료 안내, 보존기한 시작도 없다. 종결은 여전히 열린 항목을 손으로 하나씩 닫는 것이 전부다.

## 7. 시범수업 (T-01–T-04)

### T-01 시범수업 신청폼 발송·제출
- [x] **구현 완료** — 제출된 시범수업 신청이 운영자 검토에 도달한다.
- **주 전환:** 상담 결과가 시범수업 → 시범 제안 생성 → 수신자 확인 → 폼 발송 → 신청자 작성·동의 → 제출 → 운영자 검토
- **예외·복구·종료:**
  - [x] 발송 실패: 같은 유효 링크 재전달.
  - [ ] 보완 필요: 필요한 내용만 보완 요청 → 재제출.
  - [x] 링크 만료: 기존 링크 종료 → 운영자 확인 후 새 링크 발급.
  - [ ] 신청자 철회: 시범 제안 종료 → 상담 종결.
  - [x] 중복 제출: 최초 제출 결과로 수렴.
- **현행 갭:** ✅ 있음(판정 2026-08-25 · ❌ 해소) — `supabase/migrations/00018_intake_funnel.sql:61`, `supabase/migrations/00018_intake_funnel.sql:88`, `app/admin/(protected)/consultations/actions.ts:774`, `app/admin/(protected)/consultations/actions.ts:837`, `app/f/[token]/page.tsx:34`, `app/f/[token]/actions.ts:60`, `app/f/[token]/actions.ts:79`, `app/f/[token]/actions.ts:130` — 발급(원문 토큰 발송·DB에는 HMAC 해시만·기한 14일) → 수신자 판정(보호자 번호 우선) → 문자 발송 → 공개 작성 화면 → 제출 → 운영자 검토 업무까지 이어진다. 중복 제출은 status='sent' 조건을 UPDATE에 넣어 최초 제출 결과로 수렴하고(두 번 눌러도 한 번만 저장), 없는·닫힌·만료·타 테넌트 토큰은 사유를 구분하지 않고 같은 안내로 닫힌다. 발송 실패는 발급을 되돌리지 않고 링크를 화면에 돌려주며 실패분은 알림 큐가 재시도한다. 남은 갭: 원문 토큰을 보관하지 않아 「같은 유효 링크 재전달」은 구조상 불가능하고 재발급(링크 회전)이 그 자리를 대신한다, 「필요한 내용만 보완 요청 → 재제출」 경로 없음(보완도 재발급뿐), 기한 경과분을 expired로 넘기는 스윕 크론이 없어 만료는 조회 시 판정으로만 존재한다, 신청자 철회는 폼을 닫는 데서 끝나고 상담 종결로 이어지지 않는다.

### T-02 시범 일정·결제·확정
- [x] **구현 완료** — 확정 시범 회차 1개.
- **주 전환:** 시범 신청 승인 → 가능한 일정 합의 → 유료 여부 확인 → 유료이면 청구·결제 확인 → 충돌 확인 → 시범 회차 확정 → 안내
- **예외·복구·종료:**
  - [x] 일정만 합의·결제 미확인: 결제 대기, 예약 확정 아님.
  - [x] 결제 확인·일정 미확정: 일정 확정 대기.
  - [x] 무료 시범: 결제 단계를 통과 처리하지 않고 결제 불필요 근거로 일정 확정.
  - [x] 안내 실패: 회차는 유지하고 전달 재시도.
- **현행 갭:** ✅ 있음(판정 2026-08-25 · 🔶에서 상향) — `supabase/migrations/00018_intake_funnel.sql:114`, `supabase/migrations/00018_intake_funnel.sql:134`, `app/admin/(protected)/trials/actions.ts:177`, `app/admin/(protected)/trials/actions.ts:254`, `app/admin/(protected)/trials/actions.ts:437`, `app/admin/(protected)/trials/actions.ts:496`, `app/admin/(protected)/trials/actions.ts:73` — 시범 회차가 엔티티가 됐고 확정 조건이 데이터로 선다: 일정 합의와 결제 확인을 별도 플래그로 들어 「일정만 합의(결제 대기)」와 「결제만 확인(일정 확정 대기)」이 서로 다른 대기 상태로 남고, 둘이 다 서야 proposed→scheduled로 넘어간다(같은 조건을 UPDATE의 WHERE와 DB CHECK가 두 번 본다). 유료 시범은 연결된 청구가 완납일 때만 결제 확인이 서고, 무료 시범은 is_paid=false를 "결제 불필요" 근거로 남긴 채 통과한다. 확정 전 ±1시간 충돌 목록을 보여주되 강행 여부는 운영자가 고르고, 확정 안내 실패는 회차를 되돌리지 않으며 재발송 전용 경로가 따로 있다. 이 게이트 조합은 무결성 하네스 8t 절(`supabase/tests/rls/99_rls_test.sql:1469`)이 검사 대상으로 두고 있다. 남은 갭: 제출된 시범 신청폼과 회차가 데이터로 연결되지 않는다(회차 생성이 trial_sessions.form_id를 채우지 않아 항상 null — 재예약 때만 이어진다), 유료 시범의 청구서 생성 자체는 결제 화면의 별도 작업이다.

### T-03 시범 변경·취소·노쇼
- [ ] **구현 완료** — 재예약 / 취소·정산 / 노쇼 / 정상 진행 중 하나.
- **주 전환:** 변경·취소 요청 → 요청 주체·시각·귀책 확인 → 승인된 정책 판정
- **예외·복구·종료:**
  - [x] 일정 변경: 기존 회차 닫기 → 충돌 확인 → 대체 회차 생성.
  - [ ] 운영자 귀책: 무상 재예약 또는 전액 환불.
  - [ ] 신청자 취소: 정책에 따라 환불·차감·종결.
  - [x] 미참석: 10분·20분·30분 연락 → 모두 무응답이고 30분 경과 → 운영자 노쇼 확정.
  - [x] 노쇼 확정 전: 금액·등록 판단에 반영하지 않는다.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `app/admin/(protected)/trials/actions.ts:686`, `app/admin/(protected)/trials/actions.ts:740`, `app/admin/(protected)/trials/actions.ts:797`, `app/admin/(protected)/trials/actions.ts:867`, `app/admin/(protected)/trials/constants.ts:119` — 귀책과 노쇼 절차는 코드가 됐다: 취소는 요청 주체·귀책과 사유를 함께 확정하고, 재예약은 기존 회차를 닫은 뒤 충돌을 확인해 대체 회차를 만든다(유료 회차의 청구·결제 확인은 새 회차로 이어 붙고 일정 합의는 다시 받는다). 노쇼는 10·20·30분 연락 기록 3건이 append-only 감사에 남고 시작 후 30분이 지난 뒤에만 확정되며, 확정 전에는 회차 행에 어떤 표시도 남기지 않아 금액·등록 판단에 새지 않는다. 없음: 「승인된 정책 판정」 자체 — 환불·차감 금액을 정책으로 판정하거나 운영자 귀책의 무상 재예약·전액 환불을 처리하는 코드가 없어, 취소는 근거만 남기고 정산은 결제 화면의 수동 처리로 넘어간다.

### T-04 시범수업 진행·결과
- [ ] **구현 완료** — 시범 결과가 결정되어 다섯 분기 중 하나로 연결된다.
- **주 전환:** 출결 확정 → 시범수업 진행 → 수업 사실·진단 결과 기록 → 운영자 검토 → 결과 결정 → 분기(시범 결과): 정규수업 제안 → 정규수업 신청폼 → 분기(시범 결과): 재시범 → 새 시범 제안·일정 → 분기(시범 결과): 추가 확인 → 후속 상담 → 분기(시범 결과): 신청자 거절 → 상담 종결 → 분기(시범 결과): 미진행 → 상담 종결
- **예외·복구·종료:**
  - [x] 자동 판정 금지: 자동 합격·부적합 판정은 하지 않는다.
  - [x] 결과 변경: 결과가 바뀌면 이전 결과를 덮어쓰지 않고 새 결정으로 연결한다.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `supabase/migrations/00018_intake_funnel.sql:169`, `supabase/migrations/00018_intake_funnel.sql:211`, `app/admin/(protected)/trials/actions.ts:641`, `app/admin/(protected)/trials/actions.ts:979`, `app/admin/(protected)/consultations/actions.ts:798` — 결과가 회차의 컬럼이 아니라 append-only 이력이 됐다: 정본의 다섯 값(정규 제안·재시범·추가 확인·거절·미진행)을 사람이 결정할 때만 행이 쌓이고 수정·삭제는 트리거가 거부하며, 현재 결과는 가장 최근 결정이다. 출결이 확정된 뒤에만 결과를 정할 수 있고, 정규 신청폼은 최신 결과가 '정규 제안'일 때만 발급된다(그 외에는 발급 자체가 거부). 결과 이력의 수정·삭제 거부는 같은 하네스 8t 절(`supabase/tests/rls/99_rls_test.sql:1469`)이 검사 대상으로 두고 있다. 남은 갭: 수업 사실·진단 결과를 담을 자리가 결과 메모 한 줄뿐이라 진단 기록이 별도 모델로 없고, 추가 확인 → 후속 상담과 거절·미진행 → 상담 종결 두 목적지가 존재하지 않는다(후속 상담일·상담 종결 상태 부재 — C-05·C-07과 같은 구멍).

## 8. 정규수업 신청·계약·등록 (R-01–R-06)

### R-01 정규수업 신청폼 발송·제출
- [x] **구현 완료** — 제출된 정규수업 신청이 운영자 검토에 도달한다.
- **주 전환:** 상담 직접 제안 또는 시범 결과 승인 → 정규 제안 생성 → 폼 발송 → 학생·보호자·계약자·납부자·수업조건 확인 → 동의 → 제출 → 운영자 검토
- **예외·복구·종료:**
  - [ ] 보완 필요: 필요한 내용만 보완 → 재검토.
  - [x] 만료: 링크 종료 → 운영자 확인 후 재발급.
  - [ ] 철회: 정규 제안 종료 → 이미 결제한 금액이 있으면 환불 흐름.
  - [x] 미성년자에게 필요한 성인 관계가 확인되지 않음: 계약 단계 차단 → 관계 보완.
  - [ ] 기존 학생·보호자·납부자: 새 사람을 만들지 않고 확인 후 기존 관계에 연결.
- **현행 갭:** ✅ 있음(판정 2026-08-25 · ❌ 해소) — `app/admin/(protected)/consultations/actions.ts:774`, `app/admin/(protected)/consultations/actions.ts:798`, `components/public/intake/schema.ts:97`, `components/public/intake/schema.ts:117`, `components/public/intake/schema.ts:134`, `app/f/[token]/actions.ts:64`, `app/f/[token]/actions.ts:130` — 정규 신청폼이 시범과 같은 링크 규약으로 생겼고, 폼이 학생·보호자·계약자·납부자와 수업조건 희망을 함께 받는다(계약자·납부자는 '보호자와 동일'·'계약자와 동일' 체크로 관계를 표현하고, 체크를 풀면 그 역할의 이름·연락처가 필수다). 발급은 시범 결과가 '정규 제안'일 때만 열리고(시범 회차가 없는 상담은 시범 생략·직접 제안으로 통과), 종류는 링크가 정하며 서버가 DB 값으로 덮어써 시범 폼 모양으로 제출해 관계 검증을 건너뛰는 우회를 막는다. 제출은 동의와 한 문장으로 저장되고 운영자 검토 업무로 수렴한다. 남은 갭: 보완 요청 → 부분 재제출 경로가 없어 보완도 재발급(링크 회전)뿐이고, 철회 시 기수납분 환불 연결이 없으며, 제출 내용을 기존 학생·보호자·납부자와 맞추는 일은 등록 생성 때 운영자가 학생을 고르는 수동 작업이다.

### R-02 관계 확인
- [ ] **구현 완료** — 학생·보호자·계약자·납부자 관계와 학습 공유권한·청구권한 분리가 확인된다.
- **주 전환:** 제출 완료 → 학생 본인 확인 → 보호자 관계 확인 → 계약자 확인 → 납부자 확인 → 학습 공유권한·청구권한 분리 확인
- **예외·복구·종료:**
  - [ ] 보호자와 납부자가 같음: 두 역할을 한 사람에게 연결.
  - [ ] 보호자와 납부자가 다름: 학습 접근과 금전 접근을 각각 연결.
  - [ ] 성인 학생이 직접 계약·납부: 학생 역할과 금전 역할을 분리해 연결.
  - [ ] 여러 자녀: 보호자는 공유하되 각 학생 관계는 별도 유지.
  - [ ] 관계 분쟁·불명확: 등록 활성화 보류 → 운영자 확인.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `components/public/intake/schema.ts:82`, `components/public/intake/schema.ts:216`, `app/admin/(protected)/enrollments/actions.ts:218`, `app/admin/(protected)/enrollments/[id]/page.tsx:70`, `supabase/migrations/00017_role_portal.sql:64` — 네 역할이 폼 제출 스냅샷(payload.relations — 보호자·계약자·납부자와 '동일' 여부)으로 보존되고, 관계 게이트는 확인 내용을 5자 이상 필수 서술로 받아 감사에 남긴다(빈 체크박스 통과 금지). 등록 상세는 이 학생에게 열려 있는 M1 포털 관계(student·guardian·payer·contractor)를 참고 근거로 보여준다. 없음: 등록에 계약자·납부자를 구조화된 행으로 연결하는 모델 — 게이트의 근거는 운영자 서술문이고 포털 관계는 활성화 이후 발급이라 이 시점엔 비어 있을 수 있다. 학습 공유권한·청구권한 분리는 포털 관계의 역할 값으로만 존재하고 등록 게이트가 그것을 검증하지 않으며, 형제자매의 보호자 공유 관계를 확인하는 경로도 없다.

### R-03 계약 동의
- [ ] **구현 완료** — 성인 계약자의 동의로 계약이 수락된다.
- **주 전환:** 확인된 수업조건·정책으로 계약 제안 → 성인 계약자 확인 → 동의 → 계약 수락
- **예외·복구·종료:**
  - [x] 신청폼 동의와 계약 수락 구분: 신청폼 동의만으로 계약 수락 처리하지 않는다.
  - [x] 조건 변경: 기존 제안 종료 → 새 계약본 → 다시 동의.
  - [ ] 동의 거절·만료: 정규 신청 종료 또는 재협의.
  - [ ] 이미 수납된 금액: 이미 수납된 금액이 있으면 종료 전에 환불·대사.
- **현행 갭:** 🔶 부분(판정 2026-08-25 · ❌에서 상향) — `supabase/migrations/00018_intake_funnel.sql:298`, `supabase/migrations/00018_intake_funnel.sql:317`, `app/admin/(protected)/enrollments/actions.ts:285`, `app/admin/(protected)/enrollments/actions.ts:318`, `app/admin/(protected)/enrollments/actions.ts:615` — 계약이 별도 행이 됐다: 동의 시점의 수업 조건 스냅샷(terms)과 계약자 이름·연락처·동의 시각이 함께 있어야 계약 게이트가 서고, 한 등록에 동의된 계약본은 부분 유니크로 하나뿐이다 — 신청폼 동의가 계약 수락으로 승격되지 않는다. 관계 확인이 끝나기 전에는 계약을 기록할 수 없고, 조건을 바꾸려면 계약 게이트를 해제해 기존 동의를 거둔 뒤 새 계약본에 다시 동의를 받는다(계약본과 조건 스냅샷은 보존). 없음: 계약자 본인이 계약본을 열람하고 동의하는 경로 — 동의는 운영자가 대신 기록하는 값이고(terms.agreed_via='operator_record') 계약 제안 발송·성인 여부 확인·동의 거절·만료 상태가 없다. 기수납분 환불·대사 인계도 없다.

### R-04 활성화 준비
- [ ] **구현 완료** — 네 조건(계약 수락·결제 확인·일정 확정·정원 확보) 충족 시에만 등록 활성화로 진행된다.
- **주 전환:** 상태 수렴: 계약 수락 여부·결제 확인 여부·일정 확정 여부·정원 확보 여부 확인 → 네 조건 모두 충족 → 등록 활성화 → 하나라도 미완료 → 등록 준비 중 → 미완료 원인별 운영자 업무 → 완료 후 다시 활성화 조건 확인
- **예외·복구·종료:**
  - [x] 결제만 완료: 일정·정원 확인 전 활성화 금지.
  - [x] 일정만 확정: 결제 확인 전 확정 수업 안내 금지.
  - [ ] 정원 상실: 대기 또는 조건 재협의.
  - [x] 결제 결과 불명확: 대사 완료 전 활성화 금지.
- **현행 갭:** 🔶 부분(판정 2026-08-25 · ⚠️ 충돌 일부만 해소) — `supabase/migrations/00018_intake_funnel.sql:239`, `supabase/migrations/00018_intake_funnel.sql:258`, `supabase/migrations/00018_intake_funnel.sql:419`, `supabase/migrations/00018_intake_funnel.sql:436`, `app/admin/(protected)/enrollments/actions.ts:425`, `app/admin/(protected)/enrollments/actions.ts:531`, `app/admin/(protected)/enrollments/actions.ts:707`, `app/admin/(protected)/students/actions.ts:28`, `app/admin/(protected)/students/actions.ts:338` — 등록이 students.status에서 분리돼 네 게이트(관계·계약·결제·일정)를 데이터로 들고, 활성 전환은 네 조건과 정원을 UPDATE의 WHERE에 넣은 단일 문장 RPC만 한다(계수와 갱신 사이 뒤집힘 없음, 게이트가 하나라도 어긋나면 0행). 결제 게이트는 그 학생 앞 완납(paid) 청구만, 일정 게이트는 취소가 아닌 확정 회차만 근거로 인정하고, 근거가 뒤집히면 사유를 받아 게이트를 내린다. 상담→학생 원클릭 전환도 이제 'trial'로 만들어 즉시 활성이 되지 않는다. 네 게이트를 하나씩 내린 조합에서 활성이 0행임은 무결성 하네스 8u 절(`supabase/tests/rls/99_rls_test.sql:1607`)이 검사 대상으로 두고 있다. 남은 충돌: 학생 생성·수정 폼의 상태 드롭다운에 '재원(active)'가 그대로 있고 서버 화이트리스트도 이를 허용해, 등록 행 없이 학생을 활성으로 만들 수 있다(재등록 절차 reEnrollStudent도 새 등록 행을 만들지 않고 students.status만 되돌린다) — 정본 게이트가 등록 경로에서만 강제되고 학생 상태 층에는 미치지 않는다.

### R-05 등록 활성화
- [x] **구현 완료** — 활성 등록 1건, 유효 계약 1건, 첫 일정과 포털 초대 흐름 연결.
- **주 전환:** 네 조건 재확인 → 학생·관계·계약·등록·첫 수업 묶음 연결 → 전체 성공 확인 → 등록 활성 → 등록 완료 안내 → 역할별 포털 초대
- **예외·복구·종료:**
  - [x] 일부 연결 실패: 활성화 전체 취소 → 원인 수정 → 처음부터 재시도.
  - [x] 완료 안내 실패: 등록은 유지하고 전달 실패 업무 생성.
- **현행 갭:** ✅ 있음(판정 2026-08-25 · ⚠️ 충돌 해소) — `supabase/migrations/00018_intake_funnel.sql:419`, `supabase/migrations/00018_intake_funnel.sql:275`, `app/admin/(protected)/enrollments/actions.ts:707`, `app/admin/(protected)/enrollments/actions.ts:754`, `app/admin/(protected)/enrollments/actions.ts:769`, `app/admin/(protected)/students/actions.ts:685`, `supabase/migrations/00017_role_portal.sql:113` — 활성화는 게이트 재확인과 전환이 한 문장이라 반쪽 활성이 남지 않고, 한 학생의 두 번째 활성 등록은 부분 유니크가 떨어뜨려 같은 실패로 수렴한다(예외 블록에서 전부 롤백). 학생 상태 미러와 상담 상태를 이어 맞춘 뒤 등록 완료 안내를 보내고, 안내가 실패해도 활성은 유지한 채 전달 실패 업무만 만든다. 역할별 포털 초대는 M1(00017 portal_relations·portal_access_links — 관계당 살아 있는 링크 1개, 재발급 시 이전 링크 무효)로 실재하므로 '학생당 단일 토큰뿐'이라는 충돌은 해소됐다. 남은 갭: 초대를 활성화 흐름이 발급하지 않는다 — 활성 화면은 학생 상세로 보내는 링크만 두고 역할별 발급은 그 화면의 수동 작업이며, 기존 /portal/[token] 단일 링크도 병행 운영으로 남아 있다.

### R-06 활성화 전 포기·만료
- [ ] **구현 완료** — 열린 준비 상태가 정리되어 종료 또는 새 정규 제안으로 이어진다.
- **주 전환:** 계약 미동의 / 미결제 / 일정 미합의 / 정원 상실 / 고객 철회 → 열린 준비 상태 확인
- **예외·복구·종료:**
  - [ ] 돈 없음: 자리·계약·폼 종료 → 보존 흐름.
  - [ ] 돈 있음: 환불 요청 자동 연결 → 환불 완료 후 종료.
  - [ ] 일정만 있음: 잠정 일정 취소 → 고객 안내 → 종료.
  - [ ] 재개 요청: 종결 전이면 조건 재검토, 종결 후면 새 정규 제안.
- **현행 갭:** 🔶 부분(판정 2026-08-25) — `app/admin/(protected)/enrollments/actions.ts:892`, `app/admin/(protected)/enrollments/[id]/page.tsx:598`, `app/admin/(protected)/consultations/actions.ts:876`, `app/admin/(protected)/recruit/actions.ts:526`, `supabase/migrations/00018_intake_funnel.sql:245` — 준비 중 등록을 사유와 함께 닫는 경로가 생겼다: 활성 등록에는 걸리지 않고, 닫은 시각을 남겨 정산·보존의 기산점이 되며, 학생 상태 미러는 건드리지 않는다(활성화된 적이 없으므로). 열린 신청폼과 자리 제안도 각각 닫을 수 있다. 없음: 포기·만료 한 번에 열린 준비 상태를 정리하는 흐름 — 폼·자리 제안·잠정 일정은 화면을 옮겨 가며 손으로 닫아야 하고, 기수납분의 환불 요청은 자동 연결 없이 "결제 화면의 환불 흐름으로 먼저 정리하라"는 안내 문구뿐이다. 기한 경과 폼·제안을 자동으로 만료시키는 스윕도 없다.
