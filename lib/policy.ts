/**
 * 약관·개인정보 처리방침의 판(版) — 이 파일이 유일한 기준이다.
 *
 * 왜 한 곳으로 모으는가: 이 값은 consents.policy_version에 그대로 박혀 "이 사람이 무엇에
 * 동의했는지"를 증명한다. 상담 폼·신청서·후기 동의 세 군데에 각각 하드코딩돼 있던 탓에,
 * 한 곳만 고치면 같은 날 받은 동의가 서로 다른 판으로 기록될 수 있었다.
 *
 * 판을 올릴 때: 문서 본문(app/(public)/privacy·terms·refund-policy)을 고쳤다면 반드시
 * 여기 버전도 올린다. 본문이 그대로면 올리지 않는다 — 버전은 "무엇에 동의했는가"의 이름이라,
 * 내용이 같은데 이름만 달라지면 나중에 대조가 안 된다.
 *
 * 과거 기록: 2026-08-26 이전 동의는 v0.9로 남아 있다. 본문은 v1.0과 같고, 시행일이 지났는데도
 * '초안' 표기가 남아 있던 것을 정리하면서 판 이름만 올렸다.
 */

/** consents.policy_version에 기록되는 값. */
export const POLICY_VERSION = "v1.0";

/** 문서 시행일(YYYY-MM-DD). 공개 페이지 상단 표기에 쓴다. */
export const POLICY_EFFECTIVE_DATE = "2026-08-14";
